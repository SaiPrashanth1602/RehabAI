fastapi==0.138.2
uvicorn[standard]==0.51.0

pydantic==2.13.4
pydantic-settings==2.14.2

# Firebase — pinned to 6.5.0 (compatible with protobuf<5 required by mediapipe)
firebase-admin==6.5.0
google-cloud-firestore==2.27.0

# Shared with frontend — must stay <5.0.0 for mediapipe compatibility
protobuf>=4.25.3,<5.0.0

numpy==1.26.4

python-dotenv==1.2.2
python-multipart==0.0.32

requests==2.34.2

email-validator==2.3.0